{
  "name": "local-development-proxy",
  "private": true,
  "description": "Isolated tooling, independent of application and Docker dependencies",
  "engines": { "node": ">=24" },
  "dependencies": { "portless": "0.15.6" }
}
